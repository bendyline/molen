{
  "name": "@bendyline/molen-examples-cubes",
  "version": "0.0.0",
  "private": true,
  "description": "The smallest complete experience: fifty drifting cubes, one spawn command and a pinned determinism test",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5225",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "biome check src test",
    "test:unit": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@bendyline/molen-client": "workspace:*",
    "@bendyline/molen-kernel": "workspace:*",
    "@bendyline/molen-schema": "workspace:*",
    "@bendyline/molen-tooling": "workspace:*",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/three": "0.184.0",
    "typescript": "catalog:",
    "vite": "^6.4.0",
    "vitest": "catalog:"
  }
}
