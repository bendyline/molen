{
  "name": "@bendyline/molen-examples-terrain-flyover",
  "version": "0.0.0",
  "private": true,
  "description": "Chunked LOD terrain from a descriptor and a seeded heightmap, with a free-fly camera",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5225",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "biome check src test",
    "test:unit": "vitest run --passWithNoTests --exclude \"test/golden/**\"",
    "test:golden": "vitest run --config vitest.golden.config.ts"
  },
  "dependencies": {
    "@bendyline/molen-client": "workspace:*",
    "@bendyline/molen-kernel": "workspace:*",
    "@bendyline/molen-schema": "workspace:*",
    "@bendyline/molen-terrain": "workspace:*",
    "@bendyline/molen-tooling": "workspace:*",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^24",
    "@types/three": "0.184.0",
    "typescript": "catalog:",
    "vite": "^6.0.7",
    "vitest": "catalog:"
  }
}
